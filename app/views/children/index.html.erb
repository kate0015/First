<h1>Сhildren</h1>

<table class="table table-bordered">
 
  <tr>
    <th>Name</th>
    <th>Age</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @children.each do |child| %>
  <tr>
    <td><%= child.name %></td>
    <td><%= child.age %></td>
    <td><i><%= link_to 'Show', child, :class=>"text-success" %></i></td>
    <td><i><%= link_to 'Edit', edit_child_path(child) %></i></td>
    <td><i><%= link_to 'Destroy', child, method: :delete, data: { confirm: 'Are you sure?' }, :class=>"text-error" %></i></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Child', new_child_path,  :class=>"btn btn-danger"%>
<%= link_to 'Mothers ', mother1s_path,  :class=>"btn btn-danger"%>

